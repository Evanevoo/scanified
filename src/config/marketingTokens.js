/**
 * Shared marketing / public-shell tokens so MUI (NavigationBar) and Tailwind (ModernLandingPage)
 * reference the same brand values. MUI app shell inside MainLayout keeps its own theme.
 */
export const marketingTokens = {
  brandTeal: '#40B5AD',
  borderStrong: '2px solid #000000',
  surface: '#FFFFFF',
  fontStack: '"Inter", "Roboto", "Helvetica", "Arial", system-ui, sans-serif',
};
